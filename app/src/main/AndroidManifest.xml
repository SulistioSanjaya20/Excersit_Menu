<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.excersitmenu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".luasperseg"></activity>
        <activity android:name=".luassgtg" />
        <activity android:name=".luastrapes" />
        <activity android:name=".luaspp" />
        <activity android:name=".luasling" />
        <activity android:name=".kelilingtrapes" />
        <activity android:name=".kelilingsegitiga" />
        <activity android:name=".kelilingpp" />
        <activity android:name=".kelilingpersegi" />
        <activity android:name=".kelilinglingkaran" />
        <activity android:name=".main2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>